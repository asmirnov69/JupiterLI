[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pyobserve"
version = "0.1.0"
dependencies = [
    "redis",
    "nicegui",
    "plotly",
]

[project.scripts]
pyobserve = "pyobserve.cli:main"
